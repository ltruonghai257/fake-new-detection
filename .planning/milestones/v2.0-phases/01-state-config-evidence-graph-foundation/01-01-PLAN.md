---
id: "01-01"
title: "State, config, and dependency declarations"
wave: 1
depends_on: []
requirements:
  - EVGRAPH-01
  - EVGRAPH-02
  - CONFIG-01
  - CONFIG-02
  - CONFIG-03
files_modified:
  - factcheck_agents/state.py
  - factcheck_agents/config.py
  - pyproject.toml
autonomous: true
---

# Plan 01-01: State, Config & Dependency Declarations

**Goal:** Extend `state.py` with three new TypedDict fields, extend `config.py` with three env-var settings, and add `networkx` to the `agents` extra in `pyproject.toml`. No new modules. No behavior changes.

## must_haves

- `state.py` `Evidence` TypedDict has `source_tier: Literal["trusted", "flagged", "social", "unknown"]`
- `state.py` `FactCheckState` TypedDict has `evidence_graph: Optional[Any]` and `reliability_signal: Optional[bool]`
- `config.py` `Settings` has `trusted_domains`, `flagged_domains`, `reliability_threshold` fields reading from `FACTCHECK_*` env vars with correct defaults
- `pyproject.toml` `agents` extra contains `networkx>=3.0`
- Existing `pytest tests/` passes without regression

---

## Task 01-01-T1: Add `source_tier` to `Evidence` and new fields to `FactCheckState`

<task>
<id>01-01-T1</id>
<wave>1</wave>
<type>execute</type>
<action>
In `factcheck_agents/state.py`:
1. Add `Literal` to the `from typing import Annotated, Any, List, Optional, TypedDict` import line — result: `from typing import Annotated, Any, List, Literal, Optional, TypedDict`.
2. Add field `source_tier: Literal["trusted", "flagged", "social", "unknown"]` to `Evidence(TypedDict, total=False)` after `score: float`.
3. Add two fields to `FactCheckState(TypedDict, total=False)` in a new `# evidence graph` comment block after the `# evaluate agent` block:
   - `evidence_graph: Optional[Any]`
   - `reliability_signal: Optional[bool]`
</action>

<read_first>
- `factcheck_agents/state.py` — read current imports and TypedDict field layout before editing
</read_first>

<acceptance_criteria>
- `factcheck_agents/state.py` contains `Literal` in the `from typing import` line
- `factcheck_agents/state.py` `Evidence` contains line `source_tier: Literal["trusted", "flagged", "social", "unknown"]`
- `factcheck_agents/state.py` `FactCheckState` contains line `evidence_graph: Optional[Any]`
- `factcheck_agents/state.py` `FactCheckState` contains line `reliability_signal: Optional[bool]`
- `python -c "from factcheck_agents.state import Evidence, FactCheckState; e = Evidence(title='t', source_tier='trusted'); print('ok')"` exits 0
</acceptance_criteria>
</task>

---

## Task 01-01-T2: Add source-tier and reliability settings to `Settings`

<task>
<id>01-01-T2</id>
<wave>1</wave>
<type>execute</type>
<action>
In `factcheck_agents/config.py`, add three new fields to the `Settings` dataclass in a new `# ── Source tier & reliability ─────────────────────────────────────────────` comment section, after the existing `# ── Model checkpoints` block:

```
trusted_domains: str = field(
    default_factory=lambda: os.getenv(
        "FACTCHECK_TRUSTED_DOMAINS",
        "vnexpress.net,thanhnien.vn,dantri.com.vn,tuoitre.vn",
    )
)
flagged_domains: str = field(
    default_factory=lambda: os.getenv("FACTCHECK_FLAGGED_DOMAINS", "kenh14.vn")
)
reliability_threshold: float = field(
    default_factory=lambda: float(os.getenv("FACTCHECK_RELIABILITY_THRESHOLD", "0.5"))
)
```

Do not change any existing fields, imports, or the `settings = Settings()` line.
</action>

<read_first>
- `factcheck_agents/config.py` — read existing field layout and comment section style before inserting
</read_first>

<acceptance_criteria>
- `factcheck_agents/config.py` contains `trusted_domains: str = field(`
- `factcheck_agents/config.py` contains `flagged_domains: str = field(`
- `factcheck_agents/config.py` contains `reliability_threshold: float = field(`
- `factcheck_agents/config.py` contains `FACTCHECK_TRUSTED_DOMAINS`
- `factcheck_agents/config.py` contains `FACTCHECK_FLAGGED_DOMAINS`
- `factcheck_agents/config.py` contains `FACTCHECK_RELIABILITY_THRESHOLD`
- `python -c "from factcheck_agents.config import settings; assert settings.reliability_threshold == 0.5; assert 'vnexpress.net' in settings.trusted_domains; print('ok')"` exits 0
</acceptance_criteria>
</task>

---

## Task 01-01-T3: Add `networkx` to `agents` extra in `pyproject.toml`

<task>
<id>01-01-T3</id>
<wave>1</wave>
<type>execute</type>
<action>
In `pyproject.toml`, add `"networkx>=3.0"` to the `agents` optional dependency list. Insert it in alphabetical order between `"mcp>=1.2.0"` and `"openai>=1.40.0"`.

Current `agents` block:
```toml
agents = [
    "langgraph>=0.2.28",
    "langchain-core>=0.3.0",
    "langchain-openai>=0.2.0",
    "openai>=1.40.0",
    "mcp>=1.2.0",
]
```

Target state:
```toml
agents = [
    "langgraph>=0.2.28",
    "langchain-core>=0.3.0",
    "langchain-openai>=0.2.0",
    "mcp>=1.2.0",
    "networkx>=3.0",
    "openai>=1.40.0",
]
```
</action>

<read_first>
- `pyproject.toml` — read current `agents` extra block before editing
</read_first>

<acceptance_criteria>
- `pyproject.toml` contains `"networkx>=3.0"` inside the `agents = [` block
- `grep -A 20 'agents = \[' pyproject.toml | grep networkx` exits 0
</acceptance_criteria>
</task>

---

## Task 01-01-T4: Regression verification

<task>
<id>01-01-T4</id>
<wave>1</wave>
<type>execute</type>
<action>
Run `pytest tests/ -q --ignore=tests/factcheck_agents` to verify no existing tests regressed. All changes in this plan are purely additive (new TypedDict fields with `total=False`, new dataclass fields with defaults, new pyproject entry). No behavior change.
</action>

<read_first>
- `pytest.ini` — verify testpaths and addopts
</read_first>

<acceptance_criteria>
- `pytest tests/ -q --ignore=tests/factcheck_agents` exits 0
- No test failures or collection errors related to `state.py` or `config.py` imports
</acceptance_criteria>
</task>

---

## Verification Criteria

1. `factcheck_agents/state.py` contains `source_tier`, `evidence_graph`, `reliability_signal` fields.
2. `factcheck_agents/config.py` contains `trusted_domains`, `flagged_domains`, `reliability_threshold` fields with `FACTCHECK_*` env var names.
3. `pyproject.toml` `agents` block contains `networkx>=3.0`.
4. `pytest tests/ -q --ignore=tests/factcheck_agents` exits 0.
5. `python -c "from factcheck_agents.state import Evidence, FactCheckState; from factcheck_agents.config import settings"` exits 0.

---

## Artifacts this phase produces

**New symbols introduced by Plan 01-01:**

- `factcheck_agents.state.Evidence.source_tier` — new TypedDict field (`Literal["trusted", "flagged", "social", "unknown"]`)
- `factcheck_agents.state.FactCheckState.evidence_graph` — new TypedDict field (`Optional[Any]`)
- `factcheck_agents.state.FactCheckState.reliability_signal` — new TypedDict field (`Optional[bool]`)
- `factcheck_agents.config.Settings.trusted_domains` — new dataclass field (`str`)
- `factcheck_agents.config.Settings.flagged_domains` — new dataclass field (`str`)
- `factcheck_agents.config.Settings.reliability_threshold` — new dataclass field (`float`)

**No new files created.** `networkx` added to `agents` extra in `pyproject.toml`.
