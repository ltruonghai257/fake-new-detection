---
id: "05-02"
title: "prompts.py CONCLUSION_SYSTEM_PROMPT Vietnamese and binary update"
wave: 1
depends_on: []
requirements:
  - CONCL-04
files_modified:
  - factcheck_agents/prompts.py
autonomous: true
---

# Plan 05-02: prompts.py CONCLUSION_SYSTEM_PROMPT Vietnamese and Binary Update

**Goal:** Update the `CONCLUSION_SYSTEM_PROMPT` default string in `prompts.py` to instruct the LLM to write `rationale` and `recommendation` in Vietnamese. The JSON response shape (keys: `label`, `confidence`, `rationale`, `citations`, `recommendation`) is unchanged. `_BINARY_MAP` in `conclusion_agent.py` (Plan 05-01) handles the `verdict_binary` derivation — no new keys are expected from the LLM.

## must_haves

- `CONCLUSION_SYSTEM_PROMPT` default string includes the phrase "Write `rationale` and `recommendation` in Vietnamese."
- Existing JSON response keys (`label`, `confidence`, `rationale`, `citations`, `recommendation`) remain in the prompt — no keys added or removed
- Label values still listed as TRUE, FALSE, MISLEADING, UNVERIFIED (unchanged from current prompt)
- `os.getenv("FACTCHECK_CONCLUSION_PROMPT", ...)` env-var override pattern preserved — callers using the env var are unaffected
- `python -c "from factcheck_agents.prompts import CONCLUSION_SYSTEM_PROMPT; assert 'Vietnamese' in CONCLUSION_SYSTEM_PROMPT"` exits 0

---

## Task 05-02-T1: Update CONCLUSION_SYSTEM_PROMPT default string

<task>
<id>05-02-T1</id>
<wave>1</wave>
<type>execute</type>
<action>
In `factcheck_agents/prompts.py`, edit the default string passed to `os.getenv("FACTCHECK_CONCLUSION_PROMPT", ...)`.

Insert "Write `rationale` and `recommendation` in Vietnamese. " after the sentence "UNVERIFIED. Be conservative: if evidence is thin or conflicting, prefer UNVERIFIED. " and before "Respond ONLY as JSON...".

The updated prompt should read (in full):

```
"You are the lead fact-checker. You are given a claim, machine-learning "
"model predictions, and web evidence. Weigh the evidence as primary and "
"the model signals as supporting. Decide one of: TRUE, FALSE, MISLEADING, "
"UNVERIFIED. Be conservative: if evidence is thin or conflicting, prefer "
"UNVERIFIED. Write `rationale` and `recommendation` in Vietnamese. "
"Respond ONLY as JSON with keys: label, confidence (0-1), "
"rationale, citations (list of URLs), recommendation."
```

No other changes to `prompts.py` — `SEARCH_QUERY_PROMPT` and `EVALUATE_SUMMARY_PROMPT` are untouched.
</action>

<read_first>
- `factcheck_agents/prompts.py` — read full file (53 lines); locate exact current `CONCLUSION_SYSTEM_PROMPT` default string boundaries
</read_first>

<acceptance_criteria>
- `uv run python -c "from factcheck_agents.prompts import CONCLUSION_SYSTEM_PROMPT; assert 'Vietnamese' in CONCLUSION_SYSTEM_PROMPT; assert 'rationale' in CONCLUSION_SYSTEM_PROMPT; assert 'recommendation' in CONCLUSION_SYSTEM_PROMPT; assert 'label' in CONCLUSION_SYSTEM_PROMPT; print('prompt OK')"` exits 0
- `uv run python -c "from factcheck_agents.prompts import SEARCH_QUERY_PROMPT, EVALUATE_SUMMARY_PROMPT; print('other prompts unaffected')"` exits 0
</acceptance_criteria>
</task>

---

## Task 05-02-T2: Regression verification

<task>
<id>05-02-T2</id>
<wave>2</wave>
<type>execute</type>
<action>
Run the factcheck_agents test suite to confirm no regressions from the prompts.py change:

```
uv run pytest tests/factcheck_agents/ -q
```
</action>

<read_first>
- None required
</read_first>

<acceptance_criteria>
- Command exits 0
- No test failures
</acceptance_criteria>
</task>

---

## Verification Criteria

1. `CONCLUSION_SYSTEM_PROMPT` default string contains "Write `rationale` and `recommendation` in Vietnamese."
2. JSON response keys (`label`, `confidence`, `rationale`, `citations`, `recommendation`) unchanged.
3. Label values (TRUE, FALSE, MISLEADING, UNVERIFIED) unchanged.
4. `os.getenv("FACTCHECK_CONCLUSION_PROMPT", ...)` wrapper preserved — callers who set the env var still override cleanly.
5. `SEARCH_QUERY_PROMPT` and `EVALUATE_SUMMARY_PROMPT` unchanged.
6. `uv run pytest tests/factcheck_agents/ -q` exits 0.

---

## Artifacts this plan produces

**Modified:**
- `factcheck_agents/prompts.py` — `CONCLUSION_SYSTEM_PROMPT` default updated to instruct Vietnamese rationale (CONCL-04)

<threat_model>
Prompt string change only. No new external calls, no new packages, no new network endpoints.

- **T-05-04 (Prompt Injection): LLM instruction added** — "Write rationale and recommendation in Vietnamese" is a benign language instruction; it does not expand the model's authority, add tool calls, or remove safety constraints. Disposition: accept.
- **T-05-SC (Supply chain):** No new packages. Disposition: accept.
</threat_model>
