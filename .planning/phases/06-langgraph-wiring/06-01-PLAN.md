---
phase: 06-langgraph-wiring
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
    - factcheck_agents/graph.py
autonomous: true
requirements:
    - GRAPH-01
    - GRAPH-02
    - GRAPH-03

must_haves:
    truths:
        - 'graph.py replaces the `evaluate` node with `verify`'
        - '`social_search` node is present with a conditional edge from `verify`'
        - '`route_after_verify(state)` returns `social_search` when `reliability_signal=True`, `conclusion` otherwise'
        - "`add_edge('social_search', 'conclusion')` exists"
        - 'Graph compiles without error after rewiring'
    artifacts:
        - path: 'factcheck_agents/graph.py'
          provides: 'Rewired LangGraph with verify + social_search nodes'
          contains: 'route_after_verify'
    key_links:
        - from: 'factcheck_agents/graph.py'
          to: 'factcheck_agents/agents/__init__.py'
          via: 'import verify_agent, social_search_agent'
          pattern: 'verify_agent|social_search_agent'
        - from: 'route_after_verify'
          to: 'state["reliability_signal"]'
          via: 'synchronous read in routing function'
          pattern: 'reliability_signal'
---

<objective>
Rewire factcheck_agents/graph.py to replace the `evaluate` node with `verify`, add the `social_search` node, and wire the conditional edge from `verify` → `social_search`/`conclusion` via `route_after_verify`.

Purpose: Brings the graph topology in line with the v2.0 agent implementations built in Phases 3–5. The graph currently wires to `evaluate_agent` which has been superseded by `verify_agent`.
Output: Updated graph.py with correct node topology and routing.
</objective>

<execution_context>
@$HOME/.codeium/windsurf/gsd-core/workflows/execute-plan.md
@$HOME/.codeium/windsurf/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-langgraph-wiring/06-CONTEXT.md

@factcheck_agents/graph.py
@factcheck_agents/agents/**init**.py
@factcheck_agents/state.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire graph.py — replace evaluate with verify, add social_search node and conditional edge</name>
  <files>factcheck_agents/graph.py</files>
  <action>
    Make the following changes to graph.py, following the decisions in 06-CONTEXT.md:

    1. Update module-level docstring from "Search -> Evaluate -> Conclusion" to
       "Search -> Verify -> (Social?) -> Conclusion" (per D-06).

    2. Update the import line — remove `evaluate_agent`, add `verify_agent` and
       `social_search_agent` (per D-05: remove from graph.py import only;
       `__init__.py` retains its existing `evaluate_agent` export unchanged).

    3. Define `route_after_verify` as a module-level function immediately before
       `build_graph()`:
         def route_after_verify(state: FactCheckState) -> str:
             if state.get("reliability_signal"):
                 return "social_search"
             return "conclusion"
       Reads `state["reliability_signal"]` synchronously (per GRAPH-02). Returns
       the string key used in add_conditional_edges mapping.

    4. Inside `build_graph()`, replace:
         g.add_node("evaluate", evaluate_agent)
       with:
         g.add_node("verify", verify_agent)
       and add:
         g.add_node("social_search", social_search_agent)
       (per GRAPH-01)

    5. Replace:
         g.add_edge("search", "evaluate")
         g.add_edge("evaluate", "conclusion")
       with:
         g.add_edge("search", "verify")
         g.add_conditional_edges(
             "verify",
             route_after_verify,
             {"social_search": "social_search", "conclusion": "conclusion"},
         )
         g.add_edge("social_search", "conclusion")
       (per GRAPH-02, GRAPH-03)

    6. Keep `g.add_edge(conclusion, END)` and `return g.compile()` unchanged.
       The `compile()` call gains a `checkpointer=` argument in Plan 06-02.

    Final topology: START → search → verify → route_after_verify →
      social_search → conclusion → END
                              ↘ conclusion (direct, reliability_signal=False/None)

  </action>
  <verify>
    <automated>rtk uv run python -c "
from factcheck_agents.graph import build_graph, route_after_verify
from factcheck_agents.state import FactCheckState

# Topology compiles

g = build_graph()
print('build_graph() OK')

# route_after_verify routing

fake_state_true = {'reliability_signal': True}
fake_state_false = {'reliability_signal': False}
fake_state_none = {'reliability_signal': None}
assert route_after_verify(fake_state_true) == 'social_search', 'True should route to social_search'
assert route_after_verify(fake_state_false) == 'conclusion', 'False should route to conclusion'
assert route_after_verify(fake_state_none) == 'conclusion', 'None should route to conclusion'
print('route_after_verify routing OK')
print('PASS')
"
</automated>
</verify>
<done>
graph.py compiles; `evaluate` node is absent; `verify` and `social_search` nodes are
present; `route_after_verify` routes `True` → `social_search`, `False`/`None` →
`conclusion`; `add_conditional_edges` and `add_edge("social_search", "conclusion")`
are both present. GRAPH-01, GRAPH-02, GRAPH-03 satisfied.
</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                         | Description                                                                                            |
| -------------------------------- | ------------------------------------------------------------------------------------------------------ |
| verify node → route_after_verify | `reliability_signal` is written by verify_agent and read synchronously; no external input crosses here |

## STRIDE Threat Register

| Threat ID | Category  | Component                           | Disposition | Mitigation Plan                                                                                  |
| --------- | --------- | ----------------------------------- | ----------- | ------------------------------------------------------------------------------------------------ |
| T-06-01   | Tampering | route_after_verify reads state dict | accept      | state is internal pipeline data; no external actor controls `reliability_signal` at routing time |
| T-06-02   | Tampering | graph.py node imports               | accept      | all agents are internal modules; no dynamic plugin loading                                       |
| T-06-SC   | Tampering | pip/uv package installs             | accept      | no new packages installed in this plan                                                           |

</threat_model>

<verification>
Run the inline verification command from Task 1.
Additionally confirm `evaluate_agent` is NOT imported in graph.py:

uv run python -c "import ast, sys; src=open('factcheck_agents/graph.py').read(); assert 'evaluate_agent' not in src, 'evaluate_agent still imported'; print('evaluate_agent removed from graph.py OK')"
</verification>

<success_criteria>

1. `build_graph()` returns a compiled `CompiledStateGraph` without errors
2. `route_after_verify` returns `"social_search"` when `reliability_signal=True`, `"conclusion"` when `False` or `None`
3. `evaluate_agent` is no longer imported in `graph.py` (agents/**init**.py export unchanged)
4. Module docstring updated to "Search -> Verify -> (Social?) -> Conclusion"
5. GRAPH-01, GRAPH-02, GRAPH-03 all satisfied
   </success_criteria>

<output>
Create `.planning/phases/06-langgraph-wiring/06-01-SUMMARY.md` when done
</output>
