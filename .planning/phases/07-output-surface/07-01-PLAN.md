---
phase: 07-output-surface
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
    - factcheck_agents/cli.py
    - factcheck_agents/__init__.py
    - factcheck_agents/mcp_server.py
    - factcheck_agents/README.md
autonomous: true
requirements:
    - OUTPUT-01
    - OUTPUT-02
    - OUTPUT-03
    - OUTPUT-04

must_haves:
    truths:
        - '`cli.py` `_print_human()` displays `verdict_label_vi` as the primary label on the VERDICT line'
        - '`_print_human()` falls back to the old format when `verdict_label_vi` is absent from the verdict dict'
        - '`run_fact_check()` return dict contains `verdict_binary` and `verdict_label_vi` at the top level (not only inside `verdict`)'
        - '`mcp_server.py` `fact_check` return dict contains `verdict_binary` and `verdict_label_vi` alongside existing keys'
        - 'All existing keys (`verdict`, `model_results`, `evidence`, `search_queries`) still present in `fact_check` response'
        - 'OUTPUT-02 (--json) is auto-satisfied: `result["verdict"]` containing both fields is already included in the JSON dump; no code change needed'
    artifacts:
        - path: 'factcheck_agents/cli.py'
          provides: 'Updated _print_human() with Vietnamese primary label'
          contains: 'verdict_label_vi'
        - path: 'factcheck_agents/__init__.py'
          provides: 'run_fact_check() with top-level verdict_binary and verdict_label_vi'
          contains: 'verdict_binary'
        - path: 'factcheck_agents/mcp_server.py'
          provides: 'fact_check() response with verdict_binary and verdict_label_vi'
          contains: 'verdict_binary'
    key_links:
        - from: '_print_human()'
          to: 'state["verdict"]["verdict_label_vi"]'
          via: 'v.get("verdict_label_vi")'
          pattern: 'verdict_label_vi'
        - from: 'run_fact_check()'
          to: 'result["verdict"]["verdict_binary"]'
          via: 'result.get("verdict", {}).get("verdict_binary")'
          pattern: 'verdict_binary'
---

<objective>
Surface `verdict_binary` and `verdict_label_vi` at every output layer: CLI human display, Python API return dict, and MCP fact_check tool response. Also update README.md examples. All changes are additive — no existing fields removed.

Purpose: Callers can read `verdict_binary` (`"REAL"` / `"FAKE"`) and `verdict_label_vi` (`"Thật"` / `"Giả"`) without traversing nested dicts; CLI shows Vietnamese as the primary label.
Output: Updated cli.py, **init**.py, mcp_server.py, README.md.
</objective>

<execution_context>
@$HOME/.codeium/windsurf/gsd-core/workflows/execute-plan.md
@$HOME/.codeium/windsurf/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-output-surface/07-CONTEXT.md

@factcheck_agents/cli.py
@factcheck_agents/**init**.py
@factcheck_agents/mcp_server.py
@factcheck_agents/state.py
@factcheck_agents/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update `_print_human()` in cli.py to show verdict_label_vi as primary label (OUTPUT-01)</name>
  <files>factcheck_agents/cli.py</files>
  <read_first>
    - factcheck_agents/cli.py — current _print_human() implementation (lines 17–35); the VERDICT print is on line 20
    - factcheck_agents/state.py — Verdict TypedDict; both `verdict_binary` and `verdict_label_vi` are total=False (optional .get() required)
    - .planning/phases/07-output-surface/07-CONTEXT.md — D-01 (compact single-line format), D-02 (fallback)
  </read_first>
  <action>
    Replace the single `print(f"VERDICT: ...")` line in `_print_human()` (currently line 20) with a two-branch
    conditional that checks for `verdict_label_vi`:

      label_vi = v.get("verdict_label_vi")
      if label_vi:
          print(f"VERDICT: {label_vi}  ({v.get('label', '')}, confidence {v.get('confidence', 0):.2f})")
      else:
          print(f"VERDICT: {v.get('label', 'UNVERIFIED')}  (confidence {v.get('confidence', 0):.2f})")

    Place these two statements immediately after `v = result.get("verdict", {}) or {}` and before the
    `print("=" * 60)` separator line, preserving the separator line order.
    Exact format when label_vi is present: `VERDICT: Thật  (SUPPORTED, confidence 0.85)`
    Exact fallback format: `VERDICT: UNVERIFIED  (confidence 0.00)`

  </action>
  <verify>
    <automated>rtk uv run python -c "
from factcheck_agents.cli import _print_human

# Test with verdict_label_vi present (D-01)

result_vi = {'verdict': {'label': 'SUPPORTED', 'verdict_label_vi': 'Thật', 'confidence': 0.85}}
import io, sys
buf = io.StringIO()
sys.stdout = buf
\_print_human(result_vi)
sys.stdout = sys.**stdout**
out = buf.getvalue()
assert 'Thật' in out, f'verdict_label_vi not in output: {out!r}'
assert 'SUPPORTED' in out, f'4-class label not in output as parenthetical: {out!r}'
assert 'confidence 0.85' in out, f'confidence not in output: {out!r}'

# Test fallback (D-02) — no verdict_label_vi

result_old = {'verdict': {'label': 'UNVERIFIED', 'confidence': 0.0}}
buf2 = io.StringIO()
sys.stdout = buf2
\_print_human(result_old)
sys.stdout = sys.**stdout**
out2 = buf2.getvalue()
assert 'UNVERIFIED' in out2, f'fallback format broken: {out2!r}'
assert 'verdict_label_vi' not in out2, f'vi label leaked into fallback: {out2!r}'

print('PASS')
"
</automated>
</verify>
<done>
`_print_human()` shows `verdict_label_vi` as primary label with 4-class `label` and confidence in
parentheses when the field is present; falls back to old format when absent.
OUTPUT-01 satisfied. D-01 and D-02 satisfied.
</done>
</task>

<task type="auto">
  <name>Task 2: Promote verdict_binary and verdict_label_vi to top level in run_fact_check() (OUTPUT-03)</name>
  <files>factcheck_agents/__init__.py</files>
  <read_first>
    - factcheck_agents/__init__.py — current run_fact_check() (lines 28–34); currently ends with `return graph.invoke(state)`
    - .planning/phases/07-output-surface/07-CONTEXT.md — D-04 (promote via assign + mutate + return)
  </read_first>
  <action>
    In `run_fact_check()`, replace the final line `return graph.invoke(state)` with three lines:

      result = graph.invoke(state)
      result["verdict_binary"] = result.get("verdict", {}).get("verdict_binary")
      result["verdict_label_vi"] = result.get("verdict", {}).get("verdict_label_vi")
      return result

    This makes `result["verdict_binary"]` and `result["verdict_label_vi"]` accessible at the top level
    of the return dict without breaking callers that read `result["verdict"]["verdict_binary"]`.
    Use `.get()` throughout; both Verdict fields are total=False (optional).

  </action>
  <verify>
    <automated>rtk uv run python -c "
import inspect
from factcheck_agents import run_fact_check
src = inspect.getsource(run_fact_check)
assert 'verdict_binary' in src, 'verdict_binary not in run_fact_check source'
assert 'verdict_label_vi' in src, 'verdict_label_vi not in run_fact_check source'
assert 'result.get' in src, 'missing .get() pattern'
print('Source check PASS')
"
</automated>
  </verify>
  <done>
    `run_fact_check()` return dict contains `verdict_binary` and `verdict_label_vi` at top level.
    Existing callers reading `result["verdict"]` are unaffected.
    OUTPUT-03 satisfied. D-04 satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add verdict_binary and verdict_label_vi to fact_check() MCP response (OUTPUT-04)</name>
  <files>factcheck_agents/mcp_server.py</files>
  <read_first>
    - factcheck_agents/mcp_server.py — current fact_check() return dict (lines 44–50); explicitly constructs keys
    - .planning/phases/07-output-surface/07-CONTEXT.md — D-05 (add at top level of explicit dict), deferred note (evaluate_agent import on L31 is Phase 6 scope; do NOT touch it)
  </read_first>
  <action>
    In the `fact_check()` function, add two keys to the existing return dict literal after the
    `"search_queries"` key. Final dict should have:
        "statement": statement,
        "verdict": result.get("verdict", {}),
        "model_results": result.get("model_results", []),
        "evidence": result.get("evidence", []),
        "search_queries": result.get("search_queries", []),
        "verdict_binary": result.get("verdict", {}).get("verdict_binary"),
        "verdict_label_vi": result.get("verdict", {}).get("verdict_label_vi"),

    Do NOT modify the `evaluate_agent` import on line 31 — that is Phase 6 scope.
    Do NOT change any other function (search_evidence, evaluate_statement) in this file.

  </action>
  <verify>
    <automated>rtk uv run python -c "
import inspect
from factcheck_agents.mcp_server import fact_check
src = inspect.getsource(fact_check)
assert 'verdict_binary' in src, 'verdict_binary not in fact_check source'
assert 'verdict_label_vi' in src, 'verdict_label_vi not in fact_check source'
assert 'statement' in src, 'statement key removed'
assert 'verdict' in src, 'verdict key removed'
assert 'model_results' in src, 'model_results key removed'
assert 'evidence' in src, 'evidence key removed'
assert 'search_queries' in src, 'search_queries key removed'
print('PASS')
"
</automated>
  </verify>
  <done>
    `fact_check()` response dict contains `verdict_binary` and `verdict_label_vi` at top level alongside
    all existing keys. OUTPUT-04 satisfied. D-05 satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update README.md CLI output example and Python API snippet (D-06)</name>
  <files>factcheck_agents/README.md</files>
  <read_first>
    - factcheck_agents/README.md — current CLI and Python usage sections (lines 52–65); CLI example and Python snippet
    - .planning/phases/07-output-surface/07-CONTEXT.md — D-06 (update two sections only; no MCP section, no architecture section)
  </read_first>
  <action>
    Make two targeted changes to factcheck_agents/README.md:

    1. In the CLI section, update the terminal output example. After the `python -m factcheck_agents.cli "..."` line
       add a comment-style example output block showing the new VERDICT format:
         # Example output:
         # VERDICT: Thật  (SUPPORTED, confidence 0.92)

    2. In the Python section, extend the `run_fact_check()` snippet to show both new top-level fields:
         result = run_fact_check("Some claim to verify")
         print(result["verdict"])           # full verdict dict (unchanged)
         print(result["verdict_binary"])    # "REAL" or "FAKE"
         print(result["verdict_label_vi"])  # "Thật" or "Giả"

    Do NOT change the MCP server section, the layout section, the configure section, or the pipeline diagram.

  </action>
  <verify>
    <automated>rtk grep -n "verdict_label_vi\|verdict_binary\|Thật\|Giả" factcheck_agents/README.md</automated>
  </verify>
  <done>
    README.md shows updated CLI output example (Thật format) and Python snippet with both new fields.
    D-06 satisfied.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                                                                                        |
| --------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| `conclusion_agent` → output layer | `verdict_binary` and `verdict_label_vi` are written by Phase 5's rule-based code (not LLM text injection); read-only at this layer |
| CLI stdout / MCP response         | New fields are string literals `"REAL"/"FAKE"/"Thật"/"Giả"` or None; no user-controlled input passes through                       |

## STRIDE Threat Register

| Threat ID | Category        | Component                          | Disposition | Mitigation Plan                                                             |
| --------- | --------------- | ---------------------------------- | ----------- | --------------------------------------------------------------------------- |
| T-07-01   | Info Disclosure | CLI prints verdict_label_vi        | accept      | verdict data is the intended output; no sensitive data added                |
| T-07-02   | Tampering       | run_fact_check mutates result dict | accept      | mutation is post-invoke, internal only; no external actor can inject values |
| T-07-SC   | Supply Chain    | no new packages installed          | accept      | all changes are dict reads/writes in existing files                         |

No high-severity threats identified. Phase is read-from-state + format-for-output only.

</threat_model>

<verification>

Run all four task inline verification commands. Additionally:

```bash
# 1. Confirm verdict_label_vi appears in _print_human output
rtk uv run python -c "
from factcheck_agents.cli import _print_human
_print_human({'verdict': {'label': 'SUPPORTED', 'verdict_label_vi': 'Thật', 'confidence': 0.92}})
"

# 2. Confirm run_fact_check promotes top-level fields (source check)
rtk uv run python -c "
import inspect; from factcheck_agents import run_fact_check
src = inspect.getsource(run_fact_check)
assert 'verdict_binary' in src and 'verdict_label_vi' in src
print('run_fact_check source OK')
"

# 3. Confirm mcp_server fact_check has both fields (source check)
rtk uv run python -c "
import inspect; from factcheck_agents.mcp_server import fact_check
src = inspect.getsource(fact_check)
assert 'verdict_binary' in src and 'verdict_label_vi' in src
print('mcp_server fact_check source OK')
"

# 4. Confirm README updated
rtk grep -c "verdict_label_vi\|Thật" factcheck_agents/README.md
```

</verification>

<success_criteria>

1. `cli.py` `_print_human()` contains a branch on `verdict_label_vi`; running with a mock result dict containing `verdict_label_vi: "Thật"` prints `VERDICT: Thật  (SUPPORTED, confidence ...)` — OUTPUT-01
2. `__init__.py` `run_fact_check()` sets `result["verdict_binary"]` and `result["verdict_label_vi"]` before returning — OUTPUT-03
3. `mcp_server.py` `fact_check()` return dict contains `"verdict_binary"` and `"verdict_label_vi"` keys alongside all five existing keys — OUTPUT-04
4. `factcheck_agents/README.md` contains `Thật` in a CLI example and `verdict_binary`/`verdict_label_vi` in the Python snippet — D-06
5. OUTPUT-02 (--json) satisfied without code change: `result["verdict"]` already contains both fields and is included in the existing JSON dump
6. No existing keys removed from any output surface; no callers broken

</success_criteria>

<artifacts_produced>

No new symbols created. All changes are additive mutations of existing functions:

-   `factcheck_agents/cli.py` — `_print_human()` adds `verdict_label_vi` branch
-   `factcheck_agents/__init__.py` — `run_fact_check()` adds two top-level dict keys
-   `factcheck_agents/mcp_server.py` — `fact_check()` adds two dict keys to return literal
-   `factcheck_agents/README.md` — documentation update only

</artifacts_produced>

<output>
Create `.planning/phases/07-output-surface/07-01-SUMMARY.md` when done
</output>
