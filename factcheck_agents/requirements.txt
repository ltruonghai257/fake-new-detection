# factcheck_agents — standalone fact-checking module deps.
# Install on top of the project's requirements.txt (torch/transformers live there).

# ── Agent orchestration ──────────────────────────────────────────────
langgraph>=0.2.28
langchain-core>=0.3.0
langchain-openai>=0.2.0
openai>=1.40.0

# ── MCP server ───────────────────────────────────────────────────────
mcp>=1.2.0

# ── HTTP (web search: Tavily / Google CSE) ───────────────────────────
requests>=2.31.0

# python-dotenv is already in the base requirements.txt
